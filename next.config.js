/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["image.openmoviedb.com", "via.placeholder.com", "st.kp.yandex.net"],
    },
};

module.exports = nextConfig;
